use dummy as knowledge.

order = linearall.
module azc{
	%Check if there is still need for an azc (id 650)
	if bel(building(BuildingID,_,OwnerID,_,_,650,_,_)) then exit-module.
	%Check if we have budget for an azc
	if bel(money(MoneyAmount),MoneyAmount<100000) then exit-module.
	%Check if there is space for an azc

	%Adopt if all preconditions are met
	if true then adopt(buildAZC).
}