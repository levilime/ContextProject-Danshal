use dummy as knowledge.

exit=nogoals.

order = linearall.
module azc{
	%%% check if there is still need for an azc (id 650)
	if not(bel(building(Id,_,OwnerID,_,_,650,_))) then adopt(buildAZC).
	%%% check if we have budget for an azc
	
	%%% check if there is space for an azc

	%if bel(functions(FS), member([Name,Id,L],FS), member('AZC',L))
	%	then adopt build(Id, 3, square(10,10,200,10)).
}